<h1>
  <%= "Автобусы #{@from.name} – #{@to.name}" %>
</h1>
<h2>
  <%= "В расписании #{@trips.size} рейсов" %>
</h2>

<% @trips.each do |trip| %>
  <ul>
    <%= render "trip", trip: trip %>
    <% if trip.bus.services.length > 0 %>
      <%= render "services", services: trip.bus.services %>
    <% end %>
  </ul>
  <%= render "delimiter" %>
<% end %>
